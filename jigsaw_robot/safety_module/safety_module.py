# import time
# import pigpio

# from jigsaw_robot.config import (
# 	FLOOR_SWITCH_PIN
# 	)

# pi = pigpio.pi()

# if not pi.connected:
# 	raise IOError("Could not connect to PI. Maybe the daemon wasn't started?")

# pi.set_mode(FLOOR_SWITCH_PIN, pigpio.INPUT)

# """
# Callback to be called whenever the sa

# pi.callback(FLOOR_SWITCH_PIN, 1)







